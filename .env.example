# Injected automatically when you connect a Neon/Vercel Postgres database in Vercel Dashboard > Storage.
# For local dev: run `vercel env pull .env.local` after linking your project.
POSTGRES_URL=

# Secret key for the admin panel — set this in Vercel Dashboard > Settings > Environment Variables
ADMIN_KEY=changeme
